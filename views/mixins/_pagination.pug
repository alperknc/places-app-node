mixin pagination(page, pages, count)
    .pagination 
        .pagination__prev 
            if page > 1
                a(href=`/stores/page/${page - 1}`) Önceki
        .pagination__text 
            p Sayfa #{page} - Toplam Sayfa #{pages}
        .pagination__next 
            if page < pages 
                a(href=`/stores/page/${parseFloat(page) + 1}`) Sonraki